import Router from "express";

const companyRoutes = Router();

// companyRoutes.get("/is_first_signin", isFirstSignIn);

// companyRoutes.post("/google_login", signup);

// companyRoutes.post("/applicationForm", applicationFrom);

export default companyRoutes;
